sudo: false
language: elixir
elixir:
  - 1.4.2
otp_release:
  - 19.2

addons:
  postgresql: '9.4'
services:
  - postgresql

cache:
  directories:
    - _build
    - deps

branches:
  only:
    - master
